# GitHub Actions Runner Configuration
# Copy this file to .env and fill in your values

# Runner name (will be visible in GitHub)
RUNNER_NAME=laptop-runner-01

# Runner registration token from GitHub
# Get from: Repository Settings → Actions → Runners → New self-hosted runner
# Or: Organization Settings → Actions → Runners → New runner
RUNNER_TOKEN=your_runner_token_here

# Runner labels (comma-separated)
RUNNER_LABELS=self-hosted,Linux,X64

# Enable Docker support
DOCKER_ENABLED=true

# Either set RUNNER_ORG for organization-level runner:
RUNNER_ORG=your-organization

# Or set RUNNER_REPO for repository-level runner:
# RUNNER_REPO=your-org/your-repo
